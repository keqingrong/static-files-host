{
  "name": "static-files-host",
  "version": "0.0.13",
  "description": "Static files host for private use",
  "scripts": {
    "serve": "serve --cors --silent --port 8000 public"
  },
  "devDependencies": {
    "serve": "^6.5.3"
  }
}
